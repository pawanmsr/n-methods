#if !defined(COMBINATORICS)
#define COMBINATORICS

#include <vector>

namespace nm
{
    template<typename T>
    bool next_permutation(std::vector<T>& permutation);
} // namespace nm

#endif // COMBINATORICS
