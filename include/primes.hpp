#if !defined(PRIMES)
#define PRIMES

#include <vector>

namespace nm {
    template<typename T>
    std::vector<T> eratosthenes_sieve(const T n);
} // primes

namespace nm {
} // primality tests

#endif // PRIMES
