use std::collections::HashSet;

struct Graph {
    n: usize,
    directed: bool,
    adjacency_list: Vec<HashSet<usize> >,
}

impl Graph {
    fn add_edge(u: usize, v: usize) {
    }

    fn mst() {
    }
}
